#include <iostream>
#include <map>
using namespace std;

int main() {
    pair<char,string> p;
    map<string,int> m;
    while (cin >> p.first and cin >> p.second) {
        if (p.first == 'a') {
            
        }
    }
}